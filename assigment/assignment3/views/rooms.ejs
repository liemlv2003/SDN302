<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Rooms</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Available Rooms</h1>
    <table>
        <tr>
            <th>Room Number</th>
            <th>Capacity</th>
            <th>Status</th>
            <th>Price Per Hour</th>
            <th>Features</th>
        </tr>
        <% rooms.forEach(room => { %>
        <tr>
            <td><%= room.roomNumber %></td>
            <td><%= room.capacity %></td>
            <td><%= room.status %></td>
            <td><%= room.pricePerHour.toLocaleString() %> VND</td>
            <td><%= room.features.join(", ") %></td>
        </tr>
        <% }) %>
    </table>
</body>
</html>
